This directory is a placeholder (I hope:-): it contains
the modifications needed for expat to compile on WinCE.
Move the files from INTO-expat\INTO-lib into expat\lib,
the files from INTO\expat into expat and you're all set.
