{
    IBClasses = (
        {
            ACTIONS = {myAction = id; }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {loadFilter = id; playWelcome = id; showHomepage = id; showLogWindow = id; }; 
            CLASS = MyAppDelegate; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}